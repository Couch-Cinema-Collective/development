node_modules/
.next/
out/
.DS_Store
*.tsbuildinfo
.vercel
.env*
!.env.example

# Native (Capacitor / Xcode) build artifacts. The ios/ project itself is
# committed; these are generated per-machine.
ios/App/build/
ios/App/Pods/
ios/App/App/public/
ios/DerivedData/
*.xcuserstate
xcuserdata/

# Apple private keys (.p8). Never commit — these are credentials, and this
# repo is public. Downloaded once from developer.apple.com and unrecoverable.
*.p8
AuthKey_*.p8
